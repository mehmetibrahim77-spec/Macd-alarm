name: MACD Alarm

on:
  schedule:
    - cron: '5 */4 * * *'
  workflow_dispatch:

jobs:
  macd:
    runs-on: ubuntu-latest

    steps:
      - name: MACD kontrolü
        run: |
          echo "MACD sistemi çalışıyor"
